# KidsPhoneGuard 项目问题综合报告（无障碍掉权 + 限时拦截）

## 1. 报告目的

本报告用于向外部专家快速说明当前项目状态、已确认问题、未确认问题、证据链、已做改造、剩余风险与建议排查路径，目标是尽量减少二次沟通成本。

报告时间：2026-03-23  
项目路径：`V:\phone ctrl`  
目标设备：华为系 Android 设备（USB 调试已接入）

---

## 2. 项目定位与核心工作机制（简述）

KidsPhoneGuard 是“儿童终端应用访问控制”项目，核心依赖：

1. **无障碍服务**：识别应用切换并执行拦截动作（遮罩、返回/回桌面、兜底压制）。  
2. **使用统计权限**：统计应用使用时长，驱动“限时规则”触发。  
3. **前台守护服务**：持续保活、权限状态观察、异常状态提示。

关键模块：

- `GuardAccessibilityService`（核心拦截执行）  
  [GuardAccessibilityService.kt](file:///v:/phone%20ctrl/app/src/main/java/com/kidsphoneguard/service/GuardAccessibilityService.kt)
- `UsageTrackingManager`（时长统计与限时触发）  
  [UsageTrackingManager.kt](file:///v:/phone%20ctrl/app/src/main/java/com/kidsphoneguard/service/UsageTrackingManager.kt)
- `LockDecisionEngine`（统一策略决策）  
  [LockDecisionEngine.kt](file:///v:/phone%20ctrl/app/src/main/java/com/kidsphoneguard/engine/LockDecisionEngine.kt)
- `GuardForegroundService`（保活 + 权限/健康监测 + 取证）  
  [GuardForegroundService.kt](file:///v:/phone%20ctrl/app/src/main/java/com/kidsphoneguard/service/GuardForegroundService.kt)

---

## 3. 当前主要异常现象（用户实测）

1. 某些受限应用在应拦截时段或超时后，仍可继续使用。  
2. 再次打开家长端后，拦截能力可能恢复。  
3. 出现“无障碍权限掉了”的情况，表现为系统层开关关闭或服务不在启用列表。  
4. 用户体感上“像后台被杀/功能不生效”。

---

## 4. 已确认事实（证据充分）

### 4.1 无障碍掉权是“系统设置层关闭”，不是仅服务进程挂掉

实测命令结果出现：

- `settings secure accessibility_enabled = 0`
- `settings secure enabled_accessibility_services = null`

并且 `dumpsys accessibility` 显示：

- `Enabled services:{}`  
- `Bound services:{}`  
- 但 `com.kidsphoneguard/.service.GuardAccessibilityService` 仍在“已安装无障碍服务列表”中

结论：当前问题属于“系统未启用该服务”，不是 APK 中服务类消失或仅短时线程卡死。

### 4.2 掉权期间前台守护仍在运行，但守护状态持续 degraded

健康快照长期出现：

- `ae=false`
- `ar=false`
- `aMissing=true`
- `degraded=true`

说明：前台服务存活、循环在跑，但核心无障碍链路不可用。

### 4.3 早期限时链路存在误判风险（已实施改造）

历史问题点：

- 仅依赖 `UsageStats` 最近记录推断前台，可能被 launcher/系统应用扰动。  
- 时间窗判定边界与异常容错不足，存在漏拦截窗口。  

已改造：

- 前台识别：`UsageEvents` 优先，`UsageStats` 兜底。  
- 限时触发：统一走 `LockDecisionEngine`。  
- 时间窗判定：修复边界时刻与异常窗口容错。

---

## 5. 已完成改造清单（含影响范围）

### 5.1 限时链路健壮性改造（业务侧）

1. **前台识别增强**（降低错误归因到 Launcher 风险）  
   [UsageTrackingManager.kt:L163-L244](file:///v:/phone%20ctrl/app/src/main/java/com/kidsphoneguard/service/UsageTrackingManager.kt#L163-L244)
2. **时长累计上限钳制**（避免轮询抖动导致异常突增）  
   [UsageTrackingManager.kt:L119-L131](file:///v:/phone%20ctrl/app/src/main/java/com/kidsphoneguard/service/UsageTrackingManager.kt#L119-L131)
3. **统一决策触发**（LIMIT 不再分叉判定）  
   [UsageTrackingManager.kt:L138-L151](file:///v:/phone%20ctrl/app/src/main/java/com/kidsphoneguard/service/UsageTrackingManager.kt#L138-L151)
4. **时间窗边界与容错修复**  
   [LockDecisionEngine.kt:L89-L119](file:///v:/phone%20ctrl/app/src/main/java/com/kidsphoneguard/engine/LockDecisionEngine.kt#L89-L119)
5. **无障碍决策引擎延迟初始化兜底**  
   [GuardAccessibilityService.kt:L507-L519](file:///v:/phone%20ctrl/app/src/main/java/com/kidsphoneguard/service/GuardAccessibilityService.kt#L507-L519)

### 5.2 掉权实时取证链路增强（非业务逻辑改动）

1. 新增 `accessibility_forensics` 取证日志（状态切换、设置变更、生命周期关键点触发）。  
2. 新增无障碍服务生命周期信号共享（最近事件/生命周期信号）。  
3. 强制快照机制（避免状态未变时关键日志被去重漏掉）。

关键代码：

- [GuardForegroundService.kt:L281-L357](file:///v:/phone%20ctrl/app/src/main/java/com/kidsphoneguard/service/GuardForegroundService.kt#L281-L357)
- [GuardAccessibilityService.kt:L30-L47](file:///v:/phone%20ctrl/app/src/main/java/com/kidsphoneguard/service/GuardAccessibilityService.kt#L30-L47)
- [GuardAccessibilityService.kt:L332-L350](file:///v:/phone%20ctrl/app/src/main/java/com/kidsphoneguard/service/GuardAccessibilityService.kt#L332-L350)

---

## 6. 当前“明确问题”与“未明确问题”分层

### 6.1 明确问题（可以下结论）

1. **无障碍掉权真实存在且会直接导致核心能力失效。**
2. **掉权发生时通常不是进程单点崩溃，而是系统层未启用无障碍。**
3. **限时链路过去存在健壮性不足，已做结构性补强。**
4. **项目已具备掉权现场取证能力，可抓取上下文证据链。**

### 6.2 未明确问题（当前证据不足）

1. **是谁触发了无障碍关闭**（用户手动/ROM策略/第三方系统组件）尚无直接 UID 级证据。  
2. **是否存在特定 ROM 规则联动**（省电策略、辅助功能治理）尚不能定性。  
3. **触发关闭的精确瞬间与前置系统事件链**仍需下一次现场完整样本验证。

---

## 7. 风险评估（按优先级）

### P0（最高）

- **系统级无障碍关闭风险**：一旦发生，拦截链路立即降级。

### P1

- **设备 ROM 策略不可控风险**：即使业务逻辑稳定，系统仍可能重置可访问性相关状态。

### P2

- **极端场景前台识别偏差风险**：已显著降低，但仍需长时间实机回归确认。

---

## 8. 当前版本验证结果

已执行：

- `.\gradlew.bat compileDebugKotlin` ✅
- `.\gradlew.bat :app:lint` ✅

状态：可构建、可静态检查通过。  
说明：存在少量 API 弃用 warning，不影响当前问题定位结论。

---

## 9. 外部专家可直接使用的排查入口

### 9.1 现场抓取命令（建议直接复制执行）

```powershell
adb logcat -c
adb logcat -v time -s GuardForegroundService:W GuardAccessibilityService:D UsageTrackingManager:D ActivityManager:I AndroidRuntime:E
```

### 9.2 关键判读字段

- `accessibility_settings_changed`：系统无障碍开关与服务列表快照  
- `accessibility_forensics`：取证汇总（top/procImp/aHbAge/signal/ignoreBattery）  
- `degraded_state_changed`：状态切换触发点  
- `health_snapshot`：持续健康指标

### 9.3 关键判读逻辑

1. 先看 `ae`（系统无障碍总开关）是否从 1 变 0。  
2. 再看 `enabledServices` 是否移除 `com.kidsphoneguard/.service.GuardAccessibilityService`。  
3. 对齐同时间窗 `top`、`procImp`、`signal` 判断是：
   - 设置页操作路径
   - 系统策略触发路径
   - 进程级异常路径

---

## 10. 建议下一步（对外协作版）

1. **先拿一份完整掉权样本**（掉权前后 30~60 秒连续日志）。  
2. **由系统侧专家做 ROM 行为归因**（重点看 Accessibility/Power/DevicePolicy 相关系统日志）。  
3. **根据归因结果再决定工程手段**：
   - 若为设置页触发：优化引导与防误触流程。  
   - 若为 ROM 治理触发：增加厂商策略适配与系统设置教育路径。  
   - 若为进程稳定性触发：强化重连与可恢复策略。

---

## 11. 结论（供决策者快速阅读）

当前项目的核心阻断不是单一代码 bug，而是：

1. **系统层无障碍掉权问题（主矛盾）**  
2. **限时链路历史健壮性不足（次矛盾，已部分修复）**

项目目前已进入“可证据化定位”阶段：  
已能在掉权发生时抓取上下文证据，但还缺“写入方身份”这类系统级铁证。  
下一步重点应是“抓完整样本 + 系统侧归因”，而不是盲目继续业务层猜测修复。
